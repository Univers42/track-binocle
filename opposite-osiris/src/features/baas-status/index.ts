// BaaS status feature module — placeholder. The current status pinger lives
// in main.ts. Decomposition will move polling, RPC ping and the public
// `<button class="baas-status">` interactions here.

export function initBaasStatus(): void {
	// Placeholder until decomposition lands.
}
