// Consent feature module — placeholder. The current consent banner is wired
// in `src/components/gdpr/ConsentBanner.astro` plus inline logic in main.ts.
// Decomposition will move the storage + ledger calls here.

export function initConsent(): void {
	// Placeholder until decomposition lands.
}
